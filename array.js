//sum of arrays by using function//

	// function sum(a){

	// 	var sum = 0;

	// 	var i;

	// 	for(i=0; i<a.length; i++){
	// 		sum = sum + a[i];
	// 	}

	// 	return sum;
	// }
     
	// console.log('sum of the array of elements is:' + sum([6,7]));



	//sum of arrays withhout using function//
	var a =[2,4,6,8,10];
	var sum = 0;
	for(var i=0; i<a.length; i++){

		sum = sum + a[i];
	}
   console.log('sum of the array of elements is:' +sum);